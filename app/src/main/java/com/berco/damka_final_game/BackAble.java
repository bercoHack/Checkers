package com.berco.damka_final_game;

import android.view.View;

public interface BackAble {
    //interface that represents activities that have safe return to the previous activity
    void onClickBack(View view);
}
